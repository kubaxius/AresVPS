- name: Install Nginx
  ansible.builtin.apt:
    package: nginx
    update_cache: true
  changed_when: false
  when: ansible_facts["distribution"] == "Ubuntu"

- name: Enable and run Nginx
  ansible.builtin.systemd_service:
    service: nginx
    state: started
    enabled: true
