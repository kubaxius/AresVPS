---
# ansible/roles/base/handlers/main.yml
- name: Base | Regenerate locales
  ansible.builtin.command: locale-gen
  changed_when: true

- name: Base | Restart journald
  ansible.builtin.systemd_service:
    name: systemd-journald.service
    state: restarted
